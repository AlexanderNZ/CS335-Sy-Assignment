'use strict';

/**
 * @ngdoc function
 * @name securityApp.controller:PurchasebookCtrl
 * @description
 * # PurchasebookCtrl
 * Controller of the securityApp
 */
angular.module('securityApp')
  .controller('PurchasebookCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
